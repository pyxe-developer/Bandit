interface:
  display_name: "Bandit"
  short_description: "Restore context, surface blockers, and start next slice"
  default_prompt: "Use $bandit to restore Bandit context, identify missing operator input, and take the next correct bootstrap step."
