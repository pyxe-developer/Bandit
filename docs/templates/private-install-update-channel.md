# Superseded Install And Update Channel Alias

status: superseded_compatibility_alias
superseded_by: .bandit/policy/install-update-channel.json

This historical template path remains in the repository because closed
`BANDIT-071` and `BANDIT-088` evidence references it. New work should use
`docs/templates/install-update-channel.md`.

policy: .bandit/policy/install-update-channel.json
distribution_posture:
public_discovery:
public_npm_publishing:
selected_channels:
publish_authority:
version_ref_semantics:
update_source:
update_check_command:
data_minimization:
out_of_scope:
non_canonical_authority:
