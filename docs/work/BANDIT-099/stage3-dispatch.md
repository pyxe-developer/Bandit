# BANDIT-099 Stage 3 Implementation Dispatch

You are the Stage 3 Implementation Writer for `BANDIT-099`.

## Context

The Work Item is already formed, Work Item PM plan mode is recorded, and Stage
2 RED evidence is recorded.

Required source artifacts:

- `docs/work/BANDIT-099/brief.md`
- `docs/work/BANDIT-099/orchestration-plan.md`
- `docs/work/BANDIT-099/red-evidence.md`
- `docs/roadmap/CURRENT_CONTEXT.md`
- `docs/roadmap/ROADMAP.md`
- `STATUS.md`
- `CLEAN_CODE.md`
- `docs/verification/STAGE_RUBRICS.md`

## Role Boundary

- You are the non-Codex Implementation Writer.
- The Test Writer is Codex.
- You must not edit tests, test helpers, fixtures, RED evidence, acceptance
  mappings, formation evidence, orchestration-plan evidence, review evidence,
  landing evidence, UAT evidence, retrospective evidence, roadmap/current-
  context/status files, coordination logs, bootstrap-gap ledger, or PRD/source
  authority files.
- You may edit source/runtime/docs/package surfaces needed to satisfy the RED
  evidence.
- You may write Stage 3 implementation evidence:
  - `docs/work/BANDIT-099/implementation-evidence.md`
  - `docs/work/BANDIT-099/writer-report.md`

Forbidden test and PM-owned surfaces include:

- `test/init.test.mjs`
- `test/public-consumer-install-quickstart.test.mjs`
- `docs/work/BANDIT-099/red-evidence.md`
- `docs/work/BANDIT-099/orchestration-plan.md`
- `docs/work/BANDIT-099/coordination-log.jsonl`
- `.bandit/bootstrap-gaps.json`
- `docs/roadmap/CURRENT_CONTEXT.md`
- `docs/roadmap/ROADMAP.md`
- `STATUS.md`

## Implementation Target

Make the current RED tests pass with the smallest scoped implementation:

- Starter governance generated by `bandit init` must be model-agnostic. It
  should refer to Bandit roles, configured agents/providers, and
  operator-selected role owners instead of declaring Codex as the PM or
  engineering manager for arbitrary consumer repos.
- Generated starter clean-code and stage-rubric text must avoid Codex-only
  governance where Bandit roles, configured providers, or operator-selected
  agents are intended.
- `bandit init` must create day-1 onboarding guidance in a fresh consumer repo.
  The RED test expects `README.md` when no README exists.
- If a consumer repo already has `README.md`, preserve it and write clear
  Bandit-specific onboarding guidance at `docs/BANDIT_ONBOARDING.md`.
- Onboarding guidance must mention governance strictness, role ownership,
  model/provider choices, Local Qwen, and operator-owned decisions.
- Public README first-time command examples must use `npx --no-install bandit`,
  `npm exec -- bandit`, or documented npm scripts until global/PATH setup is
  explicit. Do not leave copy-pasteable first-time command blocks with bare
  `bandit init`, `bandit validate`, `bandit cockpit status --json`, `bandit
  session-context current --json`, or `bandit update-check --json`.
- Keep the existing packed-install `npx --no-install bandit init`, `validate`,
  `cockpit status --json`, and `session-context current --json` behavior
  working.
- Update package allow-list only if your implementation introduces template
  files that must be distributed.

## Required Verification

Run at minimum:

```sh
node --test test/init.test.mjs
node --test test/public-consumer-install-quickstart.test.mjs
node --test test/private-install-update-channel.test.mjs
npm run typecheck
```

If time allows, also run:

```sh
npm pack --dry-run --json
```

## Required Evidence

Write `docs/work/BANDIT-099/implementation-evidence.md` with:

- writer identity and model family;
- statement that Stage 3 made zero test-surface edits;
- implementation summary mapped to acceptance criteria;
- focused test results with command evidence;
- clean-code self-check against `CLEAN_CODE.md`;
- any remaining gaps or explicitly `none`.

Write `docs/work/BANDIT-099/writer-report.md` with:

- concise summary of source/runtime/docs surfaces inspected or edited;
- commands run and outcomes;
- whether source edits were made;
- any risks for PM acceptance.

Stop after Stage 3. Do not write review, landing, UAT, retrospective,
roadmap/current-context/status, coordination, bootstrap-gap, or closeout
artifacts.
