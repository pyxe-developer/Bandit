# BANDIT-067 Implementation Evidence

## Status

`green` for stage3_implementation. The Stage 2 RED tests now pass with
source-only changes; no test surface was edited.

work_item: BANDIT-067
role: implementation_writer
model_family: claude

## RED -> GREEN Transition

Before implementation (Stage 2 RED, focused command set):

```text
not ok 6 - cockpit view model maps live CLI payload fields into first-screen status cues
  error: expected gate_strip stage_0 through stage_6, current output stops at stage_3.
not ok 4 - browser cockpit shell renders live CLI status fields on the first screen
not ok 5 - static cockpit preview is refreshed from the current live-status work item
```

After implementation:

```text
node --test test/cockpit-view-model.test.mjs    -> pass 7 / fail 0
node --test test/cockpit-browser-shell.test.mjs -> pass 5 / fail 0
```

## Files Changed

| File | Surface | Change |
| --- | --- | --- |
| `src/state/cockpit-view-model.ts` | source | Full Stage 0-6 gate strip when coordination present; ten source-linked first-screen status cues; explicit non-green states |
| `src/cockpit/render.ts` | source | Expose `status_cues` and `gate_strip` through shell render boundary |
| `src/cockpit/browser-shell.ts` | source | Render live-status cue section and gate-strip section, source-linked, inside `main` |
| `src/cockpit/preview-status-snapshot.ts` | source (new) | Deterministic saved non-canonical `CockpitStatus` snapshot for the preview |
| `src/cockpit/generate-cockpit-preview.ts` | source (new) | Render + write `public/cockpit/index.html` from the snapshot |
| `public/cockpit/index.html` | static preview | Regenerated to BANDIT-067 live-status content; BANDIT-066 content removed |

## Acceptance Criteria Mapping

| Criterion | Evidence |
| --- | --- |
| Preview renders current status derived from `cockpit status --json`, not stale BANDIT-066 content | `public/cockpit/index.html` regenerated by `generate-cockpit-preview.ts` from `preview-status-snapshot.ts`; static preview test passes; `BANDIT-066: Browser-Served Cockpit App Shell` removed |
| Typed bounded adapter maps payload into view model without UI authority | `buildCockpitViewModel` (`src/state/cockpit-view-model.ts`) maps `CockpitStatus` into presentation data; `authority: "presentation_derived_non_canonical"`, `writes_repo_artifacts: false` |
| First screen shows phase, work item, next action, operator input, blockers/stale, gate strip, landing readiness, UAT, bootstrap gaps, coordination, improvement health, source links | Ten `status_cues` + seven-gate `gate_strip`, all rendered with source links in the browser shell; covered by `cockpit view model maps live CLI payload fields...` and `browser cockpit shell renders live CLI status fields...` |
| Missing / not-ready / not-applicable / bootstrap_gap / none / blocked rendered explicitly, not green | Cues render `not_ready`, `not_applicable`, `none`, `not_recorded`, `No blockers or stale evidence`, and `missing` gate states verbatim with sources |
| Every status traceable to source | Every cue carries `source` or `sources`; coordination cue links `docs/work/BANDIT-067/coordination-log.jsonl`; asserted by `status_cues.every(cue => cue.source || cue.sources)` |
| Guarded actions stay request-only / no browser CLI execution | Action affordances unchanged; no forms, no `localStorage/sessionStorage/indexedDB/fetch(`, no `api/merge/push/deploy/policy override`; asserted by the browser-shell authority-boundary test |
| Payload acquisition, mapping, rendering, preview generation separated | Snapshot (data) / view-model (mapping) / render+browser-shell (rendering) / generator (preview) are distinct modules |

## Verification Commands And Results

- `node --test test/cockpit-view-model.test.mjs` — pass (7).
- `node --test test/cockpit-browser-shell.test.mjs` — pass (5).
- `node --test test/cockpit-ui.test.mjs` — pass (6).
- `node --test test/cockpit-status.test.mjs` — pass.
- `npm test` — pass (519).
- `npm run typecheck` — clean.
- `npm run bandit -- validate` — `Bandit state is valid.`
- `git diff --check` — clean.

## Skipped Checks

- Stage 4/5 evidence (browser/Playwright smoke, responsive/accessibility visual
  capture, CodeRabbit pre-PR, Local Qwen, review-subject hash, UAT, land-check)
  is outside Stage 3 Writer scope.
- Static preview rendered from saved snapshot, not current live CLI, because the
  live payload advanced past the RED point (next action
  `Dispatch Stage 3 implementation...`, coordination `red_recorded`). The saved
  snapshot is non-canonical and rebuildable via
  `node src/cockpit/generate-cockpit-preview.ts`.

## Test-Surface Edit Confirmation

Zero test-surface edits by the Stage 3 Writer: no changes to tests, test
helpers, fixtures, RED evidence, acceptance mappings, formation evidence, review
evidence, landing evidence, UAT evidence, or retrospective evidence. The
Stage 2 test files modified before this dispatch were authored by the Test
Writer and were not touched.
